clear 
load('/Users/ryanlab/Desktop/AliT/Scripts/saliency_analysis/data/FixationReport.mat')

%% overview
disp(' First 5 rows: ' )
disp(RSVFixationReportOutput(1:5,:))



columns = RSVFixationReportOutput.Properties.VariableNames;

for column  = columns
   column = column{:}; %from cell to string
   sprintf( ' Now looking at column: %s ' ,column )

   if class(RSVFixationReportOutput.(column)) == 'categorical'
        sprintf( ' Number of Nans: $f ', length(find(isnan(RSVFixationReportOutput.(column)))))
   end
end